var async = require('async');

angular.module('infographicApp.controllers').controller('ListController',
    ['$scope', '$route', '$sessionStorage', '$location',
        function($scope, $route, $sessionStorage, $location) {
            var ListCtrl = this;
        }
    ]);
